spring.application.name=furhope-api

# API server
server.port=${API_PORT:8081}

# MariaDB datasource (compatible with Integration DB)
spring.datasource.url=jdbc:mariadb://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:hamza}?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
spring.datasource.username=${DB_USER:safwen}
spring.datasource.password=${DB_PASSWORD:0473821736s}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Keep startup light for phase 1
spring.sql.init.mode=never

# Request logging
spring.mvc.log-request-details=true
logging.level.com.esprit.furhope.api.config.ApiRequestLoggingInterceptor=INFO
